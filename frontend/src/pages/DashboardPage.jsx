export default function DashboardPage() {
  return (
    <div>
      <h1 style={{ color: 'hsl(var(--foreground))' }}>Dashboard</h1>
      <p style={{ color: 'hsl(var(--muted-foreground))' }}>Creator studio — Phase 9</p>
    </div>
  )
}
