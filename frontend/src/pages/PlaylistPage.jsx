export default function PlaylistPage() {
  return (
    <div>
      <h1 style={{ color: 'hsl(var(--foreground))' }}>Playlist</h1>
      <p style={{ color: 'hsl(var(--muted-foreground))' }}>Playlist view — Phase 8</p>
    </div>
  )
}
