export default function SearchResultsPage() {
  return (
    <div>
      <h1 style={{ color: 'hsl(var(--foreground))' }}>Search Results</h1>
      <p style={{ color: 'hsl(var(--muted-foreground))' }}>Search results — Phase 10</p>
    </div>
  )
}
