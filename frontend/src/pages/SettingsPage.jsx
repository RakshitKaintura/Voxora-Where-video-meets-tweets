export default function SettingsPage() {
  return (
    <div>
      <h1 style={{ color: 'hsl(var(--foreground))' }}>Settings</h1>
      <p style={{ color: 'hsl(var(--muted-foreground))' }}>Account settings — Phase 10</p>
    </div>
  )
}
