export default function WatchPage() {
  return (
    <div>
      <h1 style={{ color: 'hsl(var(--foreground))' }}>Watch</h1>
      <p style={{ color: 'hsl(var(--muted-foreground))' }}>Video player — Phase 5</p>
    </div>
  )
}
